"use client";
import { useState } from "react";
import { createClient } from "@/utils/supabase/client";
export function LoginForm(){const [email,setEmail]=useState("");const [sent,setSent]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setLoading(true);setError("");const {error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${window.location.origin}/auth/callback`}});if(error)setError(error.message);else setSent(true);setLoading(false)}if(sent)return <div className="notice"><strong>Check your inbox.</strong><br/>Your secure sign-in link is on its way.</div>;return <form onSubmit={submit}><div className="field"><label htmlFor="email">Work email</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div>{error&&<p role="alert" style={{color:"var(--coral)"}}>{error}</p>}<button disabled={loading} className="button button-primary" style={{marginTop:18}}>{loading?"Sending…":"Email me a sign-in link"}</button></form>}
