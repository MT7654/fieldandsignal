import { PageShell } from "@/components/page-shell";
import { AgentAvatar } from "@/components/agent-avatar";
import { Badge, Button, Progress } from "@/components/ui";
import { activities, agents, sampleProject } from "@/lib/demo-data";
import { ArrowRight, CalendarDays } from "lucide-react";

export default function Dashboard() { return <PageShell eyebrow="Research portfolio" title="Good decisions, in motion." description="Track active engagements, approvals and specialist availability across your autonomous research team." actions={<Button href="/projects/new">New engagement</Button>}><div className="grid-4"><div className="metric"><strong>1</strong><span>Sample engagement</span></div><div className="metric"><strong>2</strong><span>Fieldwork approvals demonstrated</span></div><div className="metric"><strong>6 / 6</strong><span>AI specialists available</span></div><div className="metric"><strong>72%</strong><span>Sample journey shown</span></div></div><div className="dashboard-grid"><section className="panel"><div className="panel-header"><h2>Research engagements</h2><Badge tone="coral">Sample workspace</Badge></div><article className="command-hero"><Badge tone="coral">Transparent sample engagement</Badge><h2 style={{font:"500 38px var(--font-serif)",margin:"18px 0 8px"}}>{sampleProject.title}</h2><p>{sampleProject.question}</p><Progress value={sampleProject.progress}/><div style={{display:"flex",justifyContent:"space-between",marginTop:9,fontSize:11}}><span>{sampleProject.phase}</span><b>{sampleProject.progress}%</b></div><p className="muted-copy">Synthetic survey previews and practice interviews are clearly separated from genuine evidence.</p><div style={{marginTop:24}}><Button href="/projects/northstar-cinemas/command-centre" variant="secondary">Open command centre</Button></div></article></section><aside className="panel"><div className="panel-header"><h2>Team availability</h2><CalendarDays size={18}/></div><div className="agent-roster">{agents.map((agent) => <div className="roster-row" key={agent.slug}><AgentAvatar slug={agent.slug} size="sm"/><div><strong>{agent.name}</strong><span>{agent.role}</span></div><i className="status-pulse"/></div>)}</div></aside></div><section className="panel" style={{marginTop:20}}><div className="panel-header"><h2>Latest sample activity</h2><Badge>Traceable actions</Badge></div>{activities.slice(0,3).map((activity) => <a className="activity-item" href={activity.href} key={activity.action}><AgentAvatar slug={activity.agent} size="sm"/><div><p>{activity.action}</p><small>{activity.time} · {activity.status}</small></div><ArrowRight size={16}/></a>)}</section></PageShell>; }
