import { PageShell } from "@/components/page-shell";
import { AgentAvatar } from "@/components/agent-avatar";
import { Badge, Button, Progress } from "@/components/ui";
import { activities, agents, sampleProject } from "@/lib/demo-data";
import { ArrowRight, CalendarDays } from "lucide-react";

export default function Dashboard(){return <PageShell eyebrow="Research portfolio" title="Good decisions, in motion." description="Track active engagements, approvals and specialist availability across your autonomous research team." actions={<Button href="/projects/new">New engagement</Button>}><div className="grid-4"><div className="metric"><strong>1</strong><span>Active engagement</span></div><div className="metric"><strong>2</strong><span>Approvals pending</span></div><div className="metric"><strong>6 / 6</strong><span>Specialists available</span></div><div className="metric"><strong>78%</strong><span>Portfolio progress</span></div></div><div className="dashboard-grid"><section className="panel"><div className="panel-header"><h2>Research engagements</h2><Badge tone="coral">Demo workspace</Badge></div><article className="command-hero"><Badge tone="coral">Demonstration engagement</Badge><h2 style={{font:"500 38px var(--font-serif)",margin:"18px 0 8px"}}>{sampleProject.title}</h2><p>{sampleProject.question}</p><Progress value={sampleProject.progress}/><div style={{display:"flex",justifyContent:"space-between",marginTop:9,fontSize:11}}><span>{sampleProject.phase}</span><b>{sampleProject.progress}%</b></div><div style={{marginTop:24}}><Button href="/projects/northstar-cinemas/command-centre" variant="secondary">Open command centre</Button></div></article></section><aside className="panel"><div className="panel-header"><h2>Team availability</h2><CalendarDays size={18}/></div><div className="agent-roster">{agents.map(a=><div className="roster-row" key={a.slug}><AgentAvatar slug={a.slug} size="sm"/><div><strong>{a.name}</strong><span>{a.role}</span></div><i className="status-pulse"/></div>)}</div></aside></div><section className="panel" style={{marginTop:20}}><div className="panel-header"><h2>Latest activity</h2><Badge>Traceable actions</Badge></div>{activities.slice(0,3).map(a=><a className="activity-item" href={a.href} key={a.action}><AgentAvatar slug={a.agent} size="sm"/><div><p>{a.action}</p><small>{a.time} · {a.status}</small></div><ArrowRight size={16}/></a>)}</section></PageShell>}
