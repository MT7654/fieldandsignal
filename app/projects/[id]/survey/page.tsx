import { PageShell } from "@/components/page-shell";
import { AgentAvatar } from "@/components/agent-avatar";
import { Badge, Button } from "@/components/ui";
import { surveyQuestions, surveyResults } from "@/lib/demo-data";
import { Copy, GripVertical } from "lucide-react";

export default function Survey(){return <PageShell eyebrow="Consumer survey · Published · Demo data" title="Ask the market, directly." description="Aisha designed a concise questionnaire around the evidence gaps. The public route accepts genuine submissions into this local demo session." actions={<Button href="/survey/northstar-demo">Open public survey</Button>}><div className="grid-4"><div className="metric"><strong>38</strong><span>Responses</span><small>Seeded demonstration data</small></div><div className="metric"><strong>86%</strong><span>Completion rate</span></div><div className="metric"><strong>3m 42s</strong><span>Median completion</span></div><div className="metric"><strong>12 / 12</strong><span>Questions approved</span></div></div><div className="dashboard-grid"><section className="panel"><div className="panel-header"><div style={{display:"flex",gap:12,alignItems:"center"}}><AgentAvatar slug="aisha-rahman"/><div><Badge>Aisha Rahman · AI Methodologist</Badge><h2 style={{marginTop:7}}>Approved questionnaire</h2></div></div><Badge>Published</Badge></div>{surveyQuestions.map((q,i)=><article className="question-card" key={q.id}><header><Badge>{q.type}</Badge><GripVertical size={17}/></header><p>{i+1}. {q.question}</p>{q.options&&<div className="options">{q.options.map(o=><span className="option" key={o}>{o}</span>)}</div>}</article>)}</section><aside><section className="panel"><div className="panel-header"><h2>Location preference</h2><Badge tone="coral">Demo n=38</Badge></div><div className="chart-bars">{surveyResults.map(r=><div className="chart-bar" key={r.name}><i style={{height:`${r.value*2}px`}}><b>{r.value}%</b></i><span>{r.name}</span></div>)}</div></section><section className="panel" style={{marginTop:20}}><Copy size={18}/><h3 style={{marginTop:12}}>Public response link</h3><code style={{fontSize:11}}>fieldandsignal.com/survey/northstar-demo</code><p style={{fontSize:12,color:"var(--muted)",marginTop:14}}>Consent required · no account · no unnecessary personal data</p></section></aside></div></PageShell>}
